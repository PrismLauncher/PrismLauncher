name: Build Application and Make Release

on:
  push:
    tags:
      - "*"

permissions: {}

jobs:
  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      # Required for Azure Trusted Signing
      id-token: write
      # Required for vcpkg binary cache
      packages: write
    with:
      build-type: Release
      environment: Release
    secrets: inherit

  create_release:
    needs: build_release
    permissions:
      contents: write
    runs-on: ubuntu-slim
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: "true"
          path: "Requiem-source"
      - name: Download artifacts
        uses: actions/download-artifact@v7
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/Requiem-source Requiem-${{ env.VERSION }}
          mv Requiem-Linux-Qt6-Portable*/Requiem-portable.tar.gz Requiem-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv Requiem-Linux-aarch64-Qt6-Portable*/Requiem-portable.tar.gz Requiem-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv Requiem-*.AppImage/Requiem-*-x86_64.AppImage Requiem-Linux-x86_64.AppImage
          mv Requiem-*.AppImage.zsync/Requiem-*-x86_64.AppImage.zsync Requiem-Linux-x86_64.AppImage.zsync
          mv Requiem-*.AppImage/Requiem-*-aarch64.AppImage Requiem-Linux-aarch64.AppImage
          mv Requiem-*.AppImage.zsync/Requiem-*-aarch64.AppImage.zsync Requiem-Linux-aarch64.AppImage.zsync
          mv Requiem-macOS*/Requiem.zip Requiem-macOS-${{ env.VERSION }}.zip

          tar --exclude='.git' -czf Requiem-${{ env.VERSION }}.tar.gz Requiem-${{ env.VERSION }}

          for d in Requiem-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="Requiem-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv Requiem-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in Requiem-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="Requiem-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv Requiem-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in Requiem-Windows-MinGW-arm64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="Requiem-Windows-MinGW-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv Requiem-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          name: Requiem ${{ env.VERSION }}
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            Requiem-Linux-x86_64.AppImage
            Requiem-Linux-x86_64.AppImage.zsync
            Requiem-Linux-aarch64.AppImage
            Requiem-Linux-aarch64.AppImage.zsync
            Requiem-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            Requiem-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
            Requiem-Windows-MinGW-w64-${{ env.VERSION }}.zip
            Requiem-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            Requiem-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            Requiem-Windows-MinGW-arm64-${{ env.VERSION }}.zip
            Requiem-Windows-MinGW-arm64-Portable-${{ env.VERSION }}.zip
            Requiem-Windows-MinGW-arm64-Setup-${{ env.VERSION }}.exe
            Requiem-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            Requiem-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            Requiem-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            Requiem-Windows-MSVC-${{ env.VERSION }}.zip
            Requiem-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            Requiem-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            Requiem-macOS-${{ env.VERSION }}.zip
            Requiem-${{ env.VERSION }}.tar.gz
